# shared-types

TypeScript types shared across all four apps (Bill, Customer, LoyaltyConfig, etc.). Not yet scaffolded.
Both backend-agent and mobile-agent read/write here — coordinate changes to existing types.
